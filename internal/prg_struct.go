package prg_struct

func DirWalk(prg_type string){
	if prg_type == "Javascript"{
		JsDirWalk()
	}
	if prg_type == "PHP"{
		return
	}
	if prg_type == "Go"{
		return
	}
	if prg_type == "Lua (Plugin)"{
		return
	}
	if prg_type == "Other"{
		return
	}
}
